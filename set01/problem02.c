//2. Write a C program to add two numbers.

#include <stdio.h>
int main()
{
    int a,b,sum;
    printf("Enter number:");
    scanf("%d",&a);
    printf("Enter number:");
    scanf("%d",&b);
    sum=a+b;
    printf("%d",sum);
}